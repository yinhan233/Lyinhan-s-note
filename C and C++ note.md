# 函数
所有没有默认值的参数必须放在左边，所有有默认值的参数必须放在右边  
设计函数的时候，通过不同的参数可以实现函数重载，但是要注意不能存在这种情况  
![[Pasted image 20260121150959.png]]
这会出现二义性报错

内联函数(inline function)
在函数定义之前加inline关键字,声明前无所谓。
编译器会直接复制粘贴内联函数，但会增加内存开销
不适合作为内联函数的情况:
- 存在复杂的循环控制语句
- 有静态变量
- 存在递归调用  
设置精度
导入iomanip
cout<<fixed << setprecision(2)
![[Pasted image 20260123142456.png]]
![[Pasted image 20260123142512.png]]