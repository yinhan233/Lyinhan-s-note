## 前置条件
### 硬件
- **ESP32-S3 开发板**
### 软件
- **ESP-IDF v5.5+** 编译环境
- **Python 3.8+**（运行知识服务器）
- **pip**（安装 Python 依赖）
### 账号
- **飞书账号**：在 [飞书开放平台](https://open.feishu.cn/) 创建应用
- **DeepSeek API Key**：从 [platform.deepseek.com](https://platform.deepseek.com/) 获取
---
## 1. 部署本地知识服务器
知识服务器提供两项服务：
- **RAG 知识检索**：基于 ChromaDB 的农业知识库，为 LLM 提供专业的盆栽养护知识
- **LLM 代理**：将 ESP32 的请求转发给 DeepSeek API，同时自动注入检索到的知识
### 1.1 安装 Python 依赖
```bash
pip install chromadb
```
### 1.2 启动服务器
```bash
python pyserver/knowledge_server.py
```
启动后输出类似：
```
MimiClaw Knowledge + LLM Proxy Server starting on 0.0.0.0:8000
Knowledge base built with X chunks
```
## 2. 申请飞书应用并配置为机器人
### 2.1 创建飞书应用
1. 登录 [飞书开放平台](https://open.feishu.cn/)
2. 点击 **创建企业自建应用**，填写应用名称
3. 进入应用详情页，在 **凭证与基础信息** 中保存：
   - **App ID**（格式如 `cli_xxxxxxxxxxxxxx`）
   - **App Secret**（格式如 `xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`）
### 2.2 配置权限
在应用设置 → **权限管理** 中添加以下权限：

| 权限                       | 说明         |
| ------------------------ | ---------- |
| `im:message`             | 读取和发送消息    |
| `im:message:send_as_bot` | 以机器人身份发送消息 |

### 2.3 启用机器人并发布

1. 进入 **机器人** 页面 → 开启 **启用机器人**
2. 进入 **版本管理与发布** → 创建版本并发布
---
## 3. 配置 mimi_config.h

打开 `main/mimi_config.h`，修改以下 4 个宏：

```c
// 知识服务器 URL（改为第 1 步部署的服务器的 IP）
#define MIMI_KNOWLEDGE_SERVER_URL "http://192.168.1.108:8000/query"

// LLM 代理 URL（改为第 1 步部署的服务器的 IP，端口与 knowledge_server.py 一致）
#define MIMI_OPENAI_API_URL "http://192.168.1.108:8000/v1/chat/completions"

// 飞书应用凭证（改为第 2 步获取的 App ID 和 App Secret）
#define MIMI_SECRET_FEISHU_APP_ID     "cli_xxxxxxxxxxxxxx"
#define MIMI_SECRET_FEISHU_APP_SECRET "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

// DeepSeek API Key（改为你的 API Key）
#define MIMI_SECRET_API_KEY "sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
```

> **说明：**
> - 以上 4 个宏在 `mimi_config.h` 中已有默认值（示例 IP 和 Key），直接搜索并替换即可。
> - `MIMI_KNOWLEDGE_SERVER_URL` 和 `MIMI_OPENAI_API_URL` 的 IP 通常填**同一台服务器**的 IP（因为 `knowledge_server.py` 同时提供知识检索和 LLM 代理两个端点）；如果分别部署在不同机器上，则分别填写。
> - `MIMI_SECRET_API_KEY` 是 DeepSeek 的 API Key，由知识服务器转发请求时使用。
### 可选：修改默认 Wi-Fi

如果不想每次都通过 Agri-XXXX 热点配网，可以在此文件中直接填入 Wi-Fi 信息：

```c
#define MIMI_SECRET_WIFI_SSID "你的WiFi名"
#define MIMI_SECRET_WIFI_PASS "你的WiFi密码"
```

---
## 4. 编译与烧录固件

### 4.1 安装 ESP-IDF v5.5 编译环境

**Ubuntu / Debian：**

```bash
cd /path/to/mimiclaw-argi
bash scripts/setup_idf_ubuntu.sh
```

该脚本会自动安装编译工具链、克隆 ESP-IDF v5.5.2 到 `~/.espressif/`，并安装 esp32s3 目标支持。

**手动安装（适用于其他系统）：**
参考 [ESP-IDF 官方文档](https://docs.espressif.com/projects/esp-idf/en/v5.5.2/esp32s3/get-started/)，安装完成后确保能执行 `idf.py --version`。
### 4.2 激活 ESP-IDF 环境
需要执行：
```bash
. ~/.espressif/esp-idf-v5.5.2/export.sh
```
### 4.3 编译
```bash
cd /path/to/mimiclaw-argi
idf.py set-target esp32s3
idf.py fullclean
idf.py build
```

编译成功后输出：`Project build complete.`
### 4.4 烧录到 ESP32-S3

用 USB 数据线连接 ESP32-S3 开发板到电脑，然后：

**查找串口：**

```bash
# Linux
ls /dev/ttyACM* /dev/ttyUSB*

# macOS
ls /dev/cu.usb*
```
常见端口：
- Linux: `/dev/ttyACM0`
- macOS: `/dev/cu.usbmodem1101`
**烧录并打开串口监视：**
```bash
idf.py -p /dev/ttyACM0 flash monitor
```
烧录完成后，串口监视器会显示启动日志。你会在日志中看到类似：
```
I (xxx) mimi: MimiClaw - ESP32-S3 AI Agent
I (xxx) wifi: WiFi connecting to HZAUAIoTLab0...
```
---
## 5. 首次 WiFi 配网（Agri-XXXX 热点）

如果第 3 步没有在 `mimi_config.h` 中填写正确的 WiFi 信息，设备首次启动时无法连接 WiFi，会自动进入配网模式。
### 5.1 连接 Agr-XXXX 热点

设备启动约 10 秒后，用手机或笔记本电脑搜索 Wi-Fi，会看到一个名为 **`Agri-XXXX`** 的开放热点（无密码），连接它。
> `XXXX` 是设备 MAC 地址的后四位，用于区分多个设备。
### 5.2 打开配网页面

连接成功后，浏览器打开：

```
http://192.168.4.1
```

会看到一个配置页面，填写以下必填项：

| 字段 | 说明 | 示例 |
|------|------|------|
| SSID | WiFi 名称 | `MyWiFi` |
| Password | WiFi 密码 | `mypassword123` |

可选填写项（如果已在 `mimi_config.h` 中配好，可以跳过）：

| 字段 | 说明 |
|------|------|
| API Key | DeepSeek API Key |
| Feishu App ID | 飞书 App ID |
| Feishu App Secret | 飞书 App Secret |
### 5.3 保存并重启
点击 **Save & Restart**。设备会保存配置到 NVS Flash，自动重启并连接你指定的 Wi-Fi。
### 5.4 验证连接
用串口监视器查看连接状态：

```bash
idf.py -p /dev/ttyACM0 monitor
```

看到 `I (xxx) wifi: WiFi connected: 192.168.x.x` 即表示连接成功。记录下这个 IP 地址。
> 如果烧录固件时已经填写了正确的 WiFi，设备会跳过配网模式直接连接，不会出现 Agri-XXXX 热点。
---
## 6. 在飞书上与盆栽对话

### 6.1 查找机器人

1. 打开飞书 App
2. 在搜索框搜索你在第 2 步创建的**应用名称**（如 "MimiClaw 盆栽助手"）
3. 点击进入与机器人的 **单聊对话**
4. 在此之后，便可使用机器人