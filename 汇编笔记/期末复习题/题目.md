# 8086 汇编语言（MASM 5.0）期末复习题

> **考试题型**：一、解析题   ; 二、编程题  
> **范围**：8086 汇编语言，基于 MASM 5.0  
> **不考察**：DEBUG 的使用

---
## 一、解析题（共 50 题）
### （一）基础知识（第 1 ~ 6 题）
**1.** 汇编语言的三种元素分别是什么？其中哪些能够被翻译为机器指令，哪些不能？  
**2.** 8086 CPU 有 \_\_\_\_ 根地址总线，最大寻址空间为 \_\_\_\_ B（即 \_\_\_\_ MB）。  
**3.** 8086 CPU 的数据总线宽度为 \_\_\_\_，控制总线的作用是 \_\_\_\_。  
**4.** 执行指令 `ADD AX, BX` 后，可能受到影响的标志位有哪些？`MOV AX, BX` 指令是否会影响标志位？  
**5.** 已知 `(DS)=2000H`，`(BX)=1000H`，则指令 `MOV AX, [BX]` 访问的物理地址是多少？  
**6.** 8086 CPU 采用 \_\_\_\_ 存储模式（大端/小端）。若在 `2000:0000` 处存放字数据 `5678H`，则 `2000:0000` 单元存放 \_\_\_\_H，`2000:0001` 单元存放 \_\_\_\_H。  

---
### （二）寄存器与标志位（第 7 ~ 16 题）
**7.** 8086 CPU 共有多少个 16 位寄存器？按功能可分为哪几类？  
**8.** 下列寄存器中，哪些可以拆分为两个 8 位寄存器使用？    
A. AX   ; B. SI   ; C. CX   ; D. DI  
**9.** 写出下列寄存器的默认用途：    
（1）AX   ;（2）BX   ;（3）CX   ;（4）DX   ;（5）SP   ;（6）BP   ;（7）SI   ;（8）DI

**10.** 执行以下指令后，`AX` 和 `DX` 的值分别是多少？  
```asm
MOV AX, 0FFFH
ADD AX, 1
```
（提示：考虑进位标志 CF，但 ADD 不会把进位加到 AX 里）

**11.** 已知 `(AL)=86H`，`(BL)=7FH`。执行 `ADD AL, BL` 后：  
（1）`AL` 的值是多少？  
（2）`CF` 的值是多少？为什么？  
（3）`SF` 的值是多少？为什么？  
（4）`OF` 的值是多少？为什么？

**12.** 已知 `(AX)=3000H`，`(BX)=5000H`。执行 `SUB AX, BX` 后：  
（1）`AX` 的值是多少？  
（2）`CF` 的值是多少？  
（3）`ZF` 的值是多少？

**13.** 执行以下指令后，`AX` 和 `BX` 的值分别是多少？`CF` 的值是多少？
```asm
MOV AX, 5678H
MOV BX, 1234H
ADD AX, 0DEF0H
ADC BX, 9ABCH
```

**14.** 执行以下指令后，`AX` 和 `DX` 的值分别是多少？`CF` 的值是多少？
```asm
MOV AX, 2000H
MOV BX, 3000H
SUB AX, BX
SBB DX, 0
```

**15.** 8086 的标志寄存器 FLAGS 中，`ZF`、`SF`、`CF`、`OF`、`PF` 分别表示什么含义？其中针对有符号数运算的标志位是哪个，针对无符号数运算的标志位是哪个？

**16.** 执行以下指令后，分别写出 `ZF` 和 `SF` 的值：
```asm
MOV AL, 0
ADD AL, 0FFH
```
（已知 `0FFH` 作为有符号数时为 -1）

---

### （三）内存寻址（第 17 ~ 26 题）

**17.** 8086 中，能够放入 `[...]` 中用于内存寻址的寄存器有哪几个？分别写出它们默认对应的段寄存器。

**18.** 下列寻址方式中，哪些是合法的？哪些是非法的？说明理由。
（1）`MOV AX, [AX]`  
（2）`MOV AX, [BX]`  
（3）`MOV AX, [BP]`  
（4）`MOV AX, [SI+DI]`  
（5）`MOV AX, [BX+SI]`  
（6）`MOV AX, [BX+BP]`

**19.** 已知 `(DS)=2000H`，`(BX)=100H`，`(SI)=50H`。分别计算以下指令访问的物理地址：
（1）`MOV AL, [BX]`  
（2）`MOV AL, [BX+SI]`  
（3）`MOV AL, [BX+200H]`  
（4）`MOV AL, [BX+SI+50H]`

**20.** 8086 有哪 5 种核心内存寻址方式？请写出每种寻址方式的汇编形式和偏移地址（EA）计算公式。

**21.** 已知 `(DS)=1000H`，`(SS)=2000H`，`(BX)=100H`，`(BP)=100H`。分别写出以下指令访问的物理地址及段寄存器来源：
（1）`MOV AL, [BX]`  
（2）`MOV AL, [BP]`

**22.** 在 MASM 5.0 中，直接写 `MOV AL, [0]` 会被汇编器错误解析为什么？应如何修正？

**23.** 指出下列指令中的错误并说明原因：
```asm
MOV [1000H], [2000H]      ; (1)
MOV DS, 1000H              ; (2)
MOV ES, DS                 ; (3)
MOV AL, 1234H              ; (4)
```

**24.** 已知数据段中有以下定义：
```asm
DATA SEGMENT
    ORG 100H
    VAR1 DB 10H, 20H, 30H
    VAR2 DW 4050H, 6070H
DATA ENDS
```
写出变量 `VAR1` 和 `VAR2` 的偏移地址，并画出内存中从 `VAR1` 开始各字节的存储内容。

**25.** 执行以下指令后，`(AX)=`？`(BX)=`？`(CX)=`？
```asm
DATA SEGMENT
    ORG 200H
    ARR DB 10H, 20H, 30H, 40H
DATA ENDS
; 假设 DS 已正确指向 DATA 段
    LEA BX, ARR
    MOV AL, [BX]
    MOV AH, 0
    MOV CX, 3
L:  ADD AL, [BX+1]
    INC BX
    LOOP L
```

**26.** 已知 `(DS)=2000H`，`(BX)=100H`，内存 `2000:0100` 处存放字节 `12H`，`2000:0101` 处存放字节 `34H`。执行以下指令后 `AX` 的值是多少？
```asm
MOV AL, [BX]
MOV AH, [BX+1]
XCHG AL, AH
```

---

### （四）堆栈操作（第 27 ~ 32 题）

**27.** 8086 的栈操作以 \_\_\_\_（字节/字）为基本单位。栈的生成方向是向 \_\_\_\_（高地址/低地址）的。

**28.** 写出 PUSH 和 POP 指令的完整执行步骤。

**29.** 已知 `(SS)=2000H`，`(SP)=0010H`。画出执行以下指令过程中 `SP` 和栈顶内容的变化（每一步）：
```asm
MOV AX, 1234H
PUSH AX
MOV BX, 5678H
PUSH BX
POP CX
POP DX
```

**30.** 下列指令序列执行后，`(AX)=`？`(SP)=`？
```asm
MOV AX, 1000H
MOV SS, AX
MOV SP, 10H
MOV AX, 1234H
PUSH AX
MOV BX, 5678H
PUSH BX
POP AX
POP BX
```

**31.** 8086 硬件是否提供栈超界保护？程序员必须如何避免栈溢出问题？

**32.** 在子程序调用中，为什么需要在子程序开头 `PUSH` 用到的寄存器，结尾按相反顺序 `POP`？如果不这样做可能导致什么问题？

---

### （五）程序结构与伪指令（第 33 ~ 38 题）

**33.** 写出下列伪指令的作用：
（1）`SEGMENT` / `ENDS`  
（2）`END`  
（3）`ASSUME`  
（4）`DB` / `DW` / `DD`

**34.** 阅读下列程序，指出其中的错误（语法错误和逻辑错误）：
```asm
ASSUME CS:CODE
CODE SEGMENT
    DB 12H, 34H, 56H
    MOV AX, 0
    MOV CX, 3
    MOV BX, 0
S:  ADD AX, [BX]
    INC BX
    LOOP S
    MOV AX, 4C10H
    INT 21H
CODE ENDS
END
```

**35.** 写出 8086 汇编程序的标准段结构模板（数据段、栈段、代码段分离）。

**36.** 伪指令 `DB 3 DUP (1, 2)` 将分配多少字节的内存空间？写出这些字节的内容。

**37.** 阅读下列程序片段，写出执行后 `(AX)=`？
```asm
DATA SEGMENT
    DW 0123H, 0456H, 0789H
DATA ENDS
; 假设 DS 已正确指向 DATA 段
    MOV BX, 0
    MOV AX, 0
    MOV CX, 3
S:  ADD AX, [BX]
    ADD BX, 2
    LOOP S
```

**38.** 如果程序开头存放的是 `DW` 定义的数据，而程序末尾未用 `END start` 指定入口，会发生什么问题？如何解决？

---

### （六）流程转移与循环（第 39 ~ 45 题）

**39.** 写出 `JMP` 指令的 5 种常见格式，并说明每种格式的转移类型（段内/段间）和原理。

**40.** `LOOP` 指令强依赖哪个寄存器？写出 `LOOP 标号` 执行时，CPU 自动完成的两个步骤。

**41.** 已知 `(CX)=0`，执行 `JCXZ NEXT` 后，程序是否会跳转到 `NEXT`？为什么？

**42.** 阅读下列程序，写出执行后 `(AX)=`？
```asm
    MOV AX, 2
    MOV CX, 4
S:  ADD AX, AX
    LOOP S
```

**43.** 在二重循环（嵌套循环）中，内外层都需要使用 `CX`，会发生什么问题？写出解决该问题的标准方案（用栈保护）。

**44.** 补全下列程序，实现计算 1+2+3+...+100 的和，结果存入 `AX`：
```asm
    MOV AX, 0
    MOV CX, 100
    MOV BX, 1
S:  _______________
    INC BX
    LOOP S
```

**45.** 阅读以下程序，写出每轮循环后 `AX` 的值：
```asm
    MOV AX, 0FFFFH
    MOV CX, 4
S:  INC AX
    LOOP S
```
（提示：注意进位，`FFFFH` 加 1 后变为 0）

---

### （七）CALL/RET 与子程序（第 46 ~ 50 题）

**46.** 写出 `CALL 标号`（段内调用）和 `RET` 指令的等价操作（入栈/出栈 + 跳转）。

**47.** 写出 `CALL FAR PTR 标号`（段间调用）和 `RETF` 指令的等价操作。

**48.** 阅读以下子程序，写出 `AX` 最终的值：
```asm
MAIN:
    MOV AX, 10
    CALL DOUBLE
    MOV BX, AX
    ; ... 
DOUBLE PROC
    SHL AX, 1
    RET
DOUBLE ENDP
```

**49.** 写出汇编语言中常见的三种参数传递方案，并简述各自优缺点。

**50.** 以下子程序的功能是将 `AX` 中的有符号数取绝对值，补全空缺部分：
```asm
ABSVAL PROC
    CMP AX, 0
    ______________   ; 如果非负，直接返回
    ______________   ; 否则取相反数
DONE:
    RET
ABSVAL ENDP
```

---

### （八）除法与标志位分析（第 51 ~ 55 题）

**51.** 阅读下列程序，写出执行后 `AX`、`DX`、`CX` 的值：
```asm
    MOV DX, 0
    MOV AX, 100
    MOV CX, 7
    DIV CX
```

**52.** `DIV` 指令中，除数为 8 位时，被除数默认存放在哪里？商和余数分别存放在哪里？除数为 16 位时呢？

**53.** 已知 `(DX)=0001H`，`(AX)=86A1H`（合起来表示 32 位被除数 186A1H = 100001），执行 `MOV BX, 100` 后执行 `DIV BX`。写出执行后 `AX` 和 `DX` 的值。

**54.** 当执行 `DIV BL` 时，如果 `BL=0`，会发生什么？此时 CPU 会触发哪一号中断？

**55.** 下列哪些指令会影响标志位？哪些不会？
（1）`MOV`   ;（2）`ADD`   ;（3）`PUSH`   ;（4）`SUB`   ;（5）`CMP`   ;（6）`INC`   ;（7）`NOT`

---

### （九）CMP 与条件转移（第 56 ~ 60 题）

**56.** `CMP` 指令的本质是什么操作？它和 `SUB` 指令的区别是什么？

**57.** 写出下列条件转移指令的判断条件和中文含义：
（1）`JE`   ;（2）`JNE`   ;（3）`JB`   ;（4）`JA`   ;（5）`JBE`

**58.** 补全下列程序，实现：若 `AX > BX`（无符号比较）则 `CX=1`，否则 `CX=0`：
```asm
    CMP AX, BX
    ______________
    MOV CX, 0
    JMP SHORT DONE
ABOVE:
    MOV CX, 1
DONE:
    ...
```

**59.** 已知 `(AL)=50H`，`(BL)=80H`。执行 `CMP AL, BL` 后：
（1）若按无符号数解释，`AL` 和 `BL` 的大小关系是？`CF` 的值是？  
（2）若按有符号数解释，`AL` 和 `BL` 的大小关系是？`OF` 的值是？`SF` 的值是？

**60.** 补全下列程序，实现：统计 `AX` 中二进制位为 1 的个数（结果存入 `CX`）：
```asm
    MOV CX, 0
AGAIN:
    CMP AX, 0
    ______________   ; 如果 AX=0 则结束
    SHR AX, 1
    ______________   ; 如果移出的位=0（CF=0），则跳过 INC
    INC CX
NEXT:
    JMP AGAIN
DONE:
    ...
```

---

### （十）串传送与中断（第 61 ~ 65 题）

**61.** 写出 `CLD` 和 `STD` 指令的功能。它们分别修改哪个标志位？

**62.** 阅读以下程序，写出其功能：
```asm
    MOV AX, DATA1
    MOV DS, AX
    MOV SI, 0
    MOV AX, DATA2
    MOV ES, AX
    MOV DI, 0
    MOV CX, 200
    CLD
    REP MOVSB
```

**63.** 中断向量表存放在内存的哪个地址区域？支持多少个中断？每个中断号的入口地址占几个字节？写出中断号为 `n` 的中断，其 `IP` 和 `CS` 的在向量表中存放地址的计算公式。

**64.** 从 `int n` 指令发出到 CPU 开始执行中断处理程序，CPU 自动完成了哪些步骤？（按顺序写出）

**65.** 中断处理程序必须以什么指令返回？它和普通的 `RET` 有什么区别？为什么不能用 `RET` 代替？

---

### （十一）综合寄存器/内存分析（第 66 ~ 72 题）

**66.** 执行以下程序段后，写出 `AX`、`BX`、`CX`、`DX`、`SP` 的值：
```asm
MOV AX, 1000H
MOV SS, AX
MOV SP, 0010H
MOV AX, 1
MOV BX, 2
MOV CX, 3
MOV DX, 4
PUSH AX
PUSH BX
PUSH CX
POP BX
POP AX
POP CX
PUSH DX
```

**67.** 已知 `(AX)=1234H`，`(BX)=5678H`，`(SP)=0100H`。执行以下指令：
```asm
PUSH AX
PUSH BX
POP AX
POP BX
```
执行后 `(AX)=`？`(BX)=`？`(SP)=`？

**68.** 阅读以下程序段，写出每一步执行后各寄存器的值（假设初始 `(AX)=0`, `(BX)=0`, `(CX)=0`)：
```asm
MOV AX, 1000H       ; (1) AX=______
MOV DS, AX          ; (2) DS=______
MOV BX, 2000H       ; (3) BX=______
MOV [BX], BX        ; (4) 物理地址 ____ 处写入字 ____
MOV AX, [BX]        ; (5) AX=______
MOV CX, [BX+2]      ; (6) CX=______（未初始化的内存）
```

**69.** 下列程序执行后，`AL` 的值是多少？（假设 `DATA` 段已正确初始化）
```asm
DATA SEGMENT
    X DB 'A'        ; ASCII 码 41H
    Y DB 'a'        ; ASCII 码 61H
DATA ENDS
    MOV AL, X
    ADD AL, Y
    SUB AL, 20H
```

**70.** 以下程序段执行后，`(AX)=`？
```asm
DATA SEGMENT
    DW 1010H, 2020H, 3030H
DATA ENDS
    LEA SI, DATA
    MOV AX, [SI]
    ADD AX, [SI+2]
```

**71.** 已知数据段定义：
```asm
ARRAY DB 10, 20, 30, 40, 50
COUNT EQU $-ARRAY
```
`COUNT` 的值是多少？`$` 的含义是什么？

**72.** 写出以下宏定义展开后的等效代码：
```asm
PRINTCHAR MACRO char
    MOV DL, char
    MOV AH, 2
    INT 21H
ENDM
; 调用
PRINTCHAR 'A'
```

---

## 二、编程题（共 28 题）

**1.** 编程实现：将数据段中 `NUM1` 和 `NUM2` 两个字节数据相加，结果存入 `RESULT` 字节单元。

**2.** 编程实现：计算数据段中三个字变量的和：`A DW 1000H, 2000H, 3000H`，结果存入 `SUM` 字变量。

**3.** 编程计算 `Y = 5X + 8`，其中 `X` 为数据段中的一个字变量，结果存入 `Y` 字变量。  
（提示：乘法可用 `MUL` 或移位/加法替代；注意 8086 的 `MUL` 为无符号乘法）

**4.** 编程实现：将 `AX` 中有符号数取绝对值后存入 `BX`。

**5.** 编程实现：比较数据段中两个字节变量 `X` 和 `Y`（无符号数），将较大的值存入 `MAX` 单元。

**6.** 编程实现：将数据段中 10 个字节数组 `ARRAY` 的元素累加，结果存入 `SUM` 字单元。

**7.** 编程实现：统计一个字变量 `NUM` 中二进制位为 1 的个数，结果存入 `COUNT` 字节单元。

**8.** 编程实现：将数据段中一个字变量 `NUM` 的高 8 位和低 8 位互换后，结果存回 `NUM`。

**9.** 编程实现：将数据段中 8 个字组成的数组 `ARR` 逆序存放（不使用额外数组，原地逆序）。

**10.** 编程实现：将 `AX` 中的值以十六进制形式显示在屏幕上。（提示：每 4 位转为一个十六进制字符，用 `INT 21H` 的 2 号功能逐字符显示）

**11.** 编程实现：在屏幕上显示数据段中定义的以 `'$'` 结尾的字符串 `MSG`。（提示：`INT 21H` 的 9 号功能）

**12.** 编程实现：将 `AL` 中的小写字母转换为大写字母（假设 `AL` 中确实为小写字母 a~z）。若 `AL` 中不是小写字母则不变。

**13.** 编程实现：将数据段中一个字符串 `STR`（以 `'$'` 结尾）中的所有小写字母转换为大写字母，其余字符不变。

**14.** 编程实现：用查表法求 `X`（0~9 的整数）的平方值，`X` 存放在字节变量中，结果存入 `RESULT` 字变量。

**15.** 编程实现：将数据段中 `SOURCE` 开始的 100 个字节数据复制到 `DEST` 开始的内存区域。  
（1）使用 `LOOP` 循环实现。  
（2）使用 `REP MOVSB` 串传送指令实现。

**16.** 编程实现：计算 1+2+3+...+N 的和，N 为数据段中的字变量（假设 N>0），结果存入 `SUM` 字单元。

**17.** 编程实现：计算 N!（N 的阶乘），N 为数据段中的字节变量（1 ≤ N ≤ 8），结果存入 `RESULT` 字单元。

**18.** 采用子程序方式，编程实现：
（1）主程序：将三个字变量 A、B、C 压栈，然后调用子程序求这三个数的最大值，结果存入 `MAX` 单元。  
（2）子程序 `MAX3`：从堆栈中取出三个数，返回最大值（通过 `AX` 返回）。

**19.** 编写子程序 `STRLEN`：计算以 `'$'` 结尾的字符串的长度（不含 `'$'`），字符串首地址通过 `SI` 传入，长度通过 `CX` 返回。

**20.** 编写子程序 `STRCPY`：将以 `'$'` 结尾的源字符串复制到目标缓冲区。源串首地址通过 `SI` 传入，目标首地址通过 `DI` 传入。要求子程序中保护所用寄存器。

**21.** 编程实现：从键盘输入一个字符（`INT 21H` 的 1 号功能），若输入的是大写字母则转换为小写字母显示，若输入的是小写字母则转换为大写字母显示，其他字符原样显示。

**22.** 编程实现：将 `AX` 中的无符号二进制数转换为 BCD 码（十进制），结果存入三个字节单元 `BCD_H`（百位）、`BCD_T`（十位）、`BCD_U`（个位）。  
（提示：使用 `DIV` 指令除以 10，余数为个位，商继续除以 10）

**23.** 编程实现：统计数据段中字节数组 `SCORE`（30 个学生的成绩）中及格（≥60 分）的人数，结果存入 `PASS` 单元；并统计不及格人数，结果存入 `FAIL` 单元。

**24.** 编程实现冒泡排序：将数据段中 `N` 个无符号字节数据按升序排列。

**25.** 编程实现：在数据段中字节数组 `ARRAY`（长度为 `N`）中查找指定的字节值 `KEY`，若找到则将位置（偏移量，从 0 开始）存入 `INDEX`，否则将 `INDEX` 置为 `-1`（即 `0FFFFH`）。

**26.** 编写一个完整的 8086 汇编程序（使用标准多段结构），实现：将数据段中的两个字变量 `DIVIDEND_L`（低字）和 `DIVIDEND_H`（高字）构成的 32 位被除数除以 16 位除数 `DIVISOR`，商存入 `QUOTIENT`，余数存入 `REMAINDER`。

**27.** 编写一个自定义的除法溢出中断处理程序（0 号中断），当发生除法溢出时，在屏幕中间显示红色字符串 `"DIVIDE ERROR!"`，然后安全返回 DOS。要求：在主程序中安装该中断处理程序，并测试触发除法溢出。

**28.** 编程实现：将从键盘输入的一串字符（以回车结束）存入缓冲区，然后将其中所有数字字符（'0'~'9'）提取出来，存入另一个缓冲区，并在末尾加上 `'$'` 后用 `INT 21H` 的 9 号功能显示。  
（提示：使用 `INT 21H` 的 `0AH` 号功能进行字符串输入）
